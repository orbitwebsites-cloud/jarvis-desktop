{
  "name": "jarvis-desktop",
  "version": "2.1.1",
  "private": true,
  "scripts": {
    "build": "node scripts/build.mjs",
    "build:vercel": "node scripts/build.mjs",
    "test": "python -m unittest discover -s tests -v",
    "build:desktop": "powershell -NoProfile -ExecutionPolicy Bypass -File ./Build-JARVIS-Portable.ps1",
    "release": "powershell -NoProfile -ExecutionPolicy Bypass -File ./scripts/Release-JARVIS.ps1"
  }
}
